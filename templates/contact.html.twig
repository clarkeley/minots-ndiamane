{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid text-center">
        <div class="row">
            <div class="col-sm-6 page-block">
                <div class="menu-background menu-op">
                    {% include 'menu/menu.html.twig' %}
                </div>

                <h1 class="d-4 font-title">CONTACT</h1>

                {#Contact form#}
                <div class="row justify-content-center d-3">
                    {{ form_start(form)}}
                    <div class="col-sm-12 contactBox">
                            {{ form_row(form.nom) }}
                    </div>
                    <div class="col-sm-12 contactBox">
                            {{ form_row(form.prenom) }}
                    </div>
                    <div class="col-sm-12 contactBox">
                            {{ form_row(form.email) }}
                    </div>
                    <div class="col-sm-12 contactBox">
                            {{ form_row(form.objet) }}
                    </div>
                    <div class="col-sm-12 contactBox">
                            {{ form_row(form.message) }}
                    </div>
                    <div class="col-sm-12 font-txt">
                        <p>Aucune information personnelle communiquée<br>dans ce formulaire ne sera transmise à un tiers ou<br>utilisée à des fins marketing sauf demande<br>expresse de votre part.</p>
                    </div>
                    <div class="col-sm-12 d-5">
                        <button type="submit" formmethod="post" class="btn btn-orange">ENVOYER</button>
                    </div>
                    {{ form_end(form) }}
                </div>

                <footer class="footer-black d-3"> Les Minots de Ndiamane &copy; 2019</footer>
            </div>

            <div class="col-sm-6 img-split p-0 contact">
                <div class="menu-background">
                    {% include 'menu/menu.html.twig' %}
                </div>

                <footer class="d-10 footer"> Les Minots de Ndiamane &copy; 2019</footer>
            </div>
        </div>
    </div>
{% endblock %}